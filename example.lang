# This is a very useful piece of software

# def oopify(prefix) -> prefix + "oop"
# return not wrkking

# len not working 

def join(elements, separator)
	let result = ""
	let len = len(elements)
end

def map(elements, func)
	let new_elements = []
end

let l = "lol"
#if l == "lol" then println("HI") end
println("LOl")

#for i = 0 to 5 then
#	println(join(map(["l", "sp"], oopify), ", "))
#end
